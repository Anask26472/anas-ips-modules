# Open-source release checklist

Before pushing to GitHub:
- remove local secrets
- remove private keys
- keep only public verification keys
- keep `.gitignore` active
- verify tests pass
- verify README matches real behavior
- tag a release only after local validation
